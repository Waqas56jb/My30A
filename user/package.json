{
  "name": "my30a-guest",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "description": "My30A Host — guest-facing experience with Vitoria, the 30A AI concierge (frontend only, mock data).",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "test": "npm run test:routes && npm run test:flows",
    "test:build": "vite build --ssr scripts/smoke.jsx --outDir .smoke/routes && vite build --ssr scripts/flows.jsx --outDir .smoke/flows",
    "test:routes": "npm run test:build && node --import ./scripts/setup-dom.js .smoke/routes/smoke.js",
    "test:flows": "node --import ./scripts/setup-dom.js .smoke/flows/flows.js",
    "test:images": "node scripts/check-images.mjs"
  },
  "dependencies": {
    "react": "^18.3.1",
    "react-dom": "^18.3.1",
    "react-markdown": "^10.1.0",
    "react-router-dom": "^6.26.2",
    "remark-breaks": "^4.0.0"
  },
  "devDependencies": {
    "@vitejs/plugin-react": "^4.3.1",
    "jsdom": "^30.0.1",
    "vite": "^5.4.8"
  }
}
